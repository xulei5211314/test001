#!/bin/sh

# show power off start timing
cat /proc/uptime

# kill "dms-sysMng-workflow" related processes
#unset PIDLIST
#PIDLIST=`ps | grep [dm]s-sysMng-workflow | awk '{print $1}'`
#for i in ${PIDLIST}
#do
 # kill -9 $i
  #while [ "$?" -ne 0 ];
  #do
   # kill -9 $i
  do#ne
done#
# kill "hobot" related processes
unset PIDLIST
PIDLIST=`ps | grep [h]obot | awk '{ print $1 }'`
#for i in ${PIDLIST}
#do
 # kill -9 $i
 # while [ "$?" -ne 0 ];
 # do
  #  kill -9 $i
kill -9 ${PIDLIST} 
 # done
#done

usleep 100000

# shutdown J2
#shutdown -h now